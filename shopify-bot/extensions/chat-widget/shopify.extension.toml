# Theme app extension.
#
# App embed blocks are the supported way to put a floating chat bubble on a
# storefront. Script tags are the old way: none can be created after
# 2026-10-01 and they stop running entirely on 2027-03-01.
name = "chat-widget"
type = "theme"
